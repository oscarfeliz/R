---
title: "AE1"
output: html_document
date: "2024-12-16"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(readr)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

## Pregunta 1

1.  Es del tipo inferencial, a partir de una muestra que se recoge se quiere generalizar al total de los vehículos que van a circular.
2.  Es un tipo exploratoria,se quiere relacionar un comportamiento por género y rango de edad.
3.  Es predictiva y causal, dado que predice un comportamiento de un operador y la causa del porqué de los fallos.
4.  Es exploratoria porque a partir de un historial de compras lo relaciona a algún grupo.

## Pregunta 2

## part 3

```{r}
epa_http <- read_table("~/Downloads/epa-http.csv", col_names = FALSE)
nrow(epa_http)
```
